# Notule — 26 mei 2026

## Aanwezig
- K. Jako
- OWL (AI-assistent)

## Overzicht

Dag van onderzoek, debugging en refactoring van het acquisitie-systeem.

---

## 1. Follow-up script bugs (opgelost)

Meerdere bugs in `followup.py` geïdentificeerd en opgelost:

1. **IMAP count check onbetrouwbaar** — Gmail bevat meerdere headers per thread, waardoor `count >= 2` ondanks maar 1 verzonden mail. IMAP duplicate-check volledig verwijderd.
2. **write_csv buiten loop** — CSV werd pas na alle mails geschreven. Bij crash gingen alle wijzigingen verloren. Nu na elke send binnen de loop.
3. **get_sent_today_from_log()** — las alleen de laatste run. Nu alle runs van vandaag (window-based).
4. **subprocess import miste** — `import subprocess` toegevoegd.

## 2. Template strategie (beleidswijziging)

**Besluit:** Geen persoonlijke/AI-gegenererde mails meer. Alleen standaard-templates.

**Reden:** K. Jako vond de AI-gegenerereerde observaties onprofessioneel en generiek. Standaard-templates werken beter.

**Implementatie:**
- 3 NL templates: "3D-visualisaties voor uw projecten", "Uw projecten sterker presenteren met 3D-visualisaties", "Visualisaties voor toekomstige projecten"
- 3 DE templates: "3D-Visualisierungen für Ihre Projekte", "Ihre Projekte stärker präsentieren mit 3D-Visualisierungen", "Visualisierungen für zukünftige Projekte"
- Roulerend: Template 1 → 2 → 3 → 1 → ...
- AI-observatie logica volledig uitgeschakeld
- Website scraping voor observaties verwijderd

## 3. Follow-up queue (eod 26-05)

- Queue: 0 follow-ups klaar
- Alle 184 "Eerste mail verzonden" bedrijven geblokkeerd via `Niet opnieuw mailen vóór` (+90 dagen)
- Volgende batch komt beschikbaar zodra de blokdatum verloopt

## 4. Counter bug

- `increment_counter()` werd ook aangeroepen bij IMAP-skip → counter liep vooruit zonder daadwerkelijke send
- Gecorrigeerd: alleen in `if ok:` branch
- `daily_counter.json` gecorrigeerd van 23 → 1

## 5. Code infrastructuur

- Gedeelde code (Lock, DailyCounter, Logger, ImapSentCache) verplaatst naar `infra.py`
- Beide scripts (v6.py + followup.py) importeren vanuit `infra.py`
- Oude scripts in `.hermes/scripts/` verwijderd

## 6. Terugkoppeling

- 1 reactie ontvangen van Tagebau (info@tagebau.de): "Nein, gibt es momentan leider niet."
- Negatief, maar nuttige feedback

---

## Actiepunten (opgelost)

- [x] Followup.py bugs fixen
- [x] Standaard-templates in beide scripts zetten
- [x] AI/observatie logica uitschakelen
- [x] Code cleanup: infra.py
- [x] Counter bug fixen

## Vandaag

- 6 follow-ups verstuurd met nieuwe standaard-templates
- Cron jobs voor v6.py gepauzeerd (worden morgen hervat)
